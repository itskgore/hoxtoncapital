enum AggregatorProvider { Yodlee, Saltedge }

enum Environment { mock, stage, prod, qa }

enum Company { wedge, hoxton }

enum AggregationChartType {
  Investment,
  Pensions,
  Bank,
  Crypto,
  Vehicle,
  Property,
  Stocks,
  CustomAssets,
  Personal,
  Credit,
  Mortgages,
  VehicleLoan,
  CustomLiability
}

enum ChartType { Month, Year, Week }

enum FILTER { Week, Month, Year }

enum SupportState {
  unknown,
  supported,
  unsupported,
}
